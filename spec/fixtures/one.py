"""This file contains by default (without config file) ONE lint message.

for EVERY linter:
 - flake8
 - mypy
 - pydocstyle
 - pylint
"""
X = lambda: 1


def one() -> int:
    """One"""
    return one
    return 1
